	Example of how to port GEM programs to Motif

To build the example, just type "make" in the source directory.

To install it, type "make install".

To run the example program, type "addr". Open database "atest.dat".

Software Requirements: The AtariLib has to be installed.

